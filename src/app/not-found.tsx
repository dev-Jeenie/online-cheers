import { Box, Button, Typography } from "@nwaycorp/global-designsystem";

const NotFound = () => {
  return (
    <Box>
      <Typography variant="h1">NotFound</Typography>
    </Box>
  );
};

export default NotFound;
