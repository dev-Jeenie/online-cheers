import { Box, Typography } from "@nwaycorp/global-designsystem";

export default function PhotoModal({ params }: { params: any }) {
  return (
    <Box>
      <Typography variant="h1">PhotoModal</Typography>
    </Box>
  );
}
