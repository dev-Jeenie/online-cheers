import { Box, Typography } from "@nwaycorp/global-designsystem";

export default function PhotoPage({ params }: { params: any }) {
  console.log("포토페이지 로드됨")
  return (
    <Box>
      <Typography variant="h1">PhotoPage</Typography>
      <Typography variant="h1">PhotoPage</Typography>
      <Typography variant="h1">PhotoPage</Typography>
      <Typography variant="h1">PhotoPage</Typography>
    </Box>
  );
}
